[Unit]
Description=Formation Git repository over Smart HTTP
After=network.target

[Service]
WorkingDirectory=@WORKSPACE@
ExecStart=@PYTHON@ @SERVER@ --port 8000 --bind 0.0.0.0
Restart=on-failure
RestartSec=5
